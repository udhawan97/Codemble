# Architecture Decision Records

One file per significant decision, numbered `NNNN-title.md`. Seed decisions are
in `CLAUDE.md` §Decision Log; promote a log entry to an ADR when it needs more
context than a table row.
